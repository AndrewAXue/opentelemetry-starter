steps:
# Build the container image
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/aaxue-starter/jaeger', '.']
# Push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/aaxue-starter/jaeger']
# Deploy container image to Cloud Run
- name: 'gcr.io/cloud-builders/gcloud'
  args: ['run', 'deploy', 'jaeger', '--image', 'gcr.io/aaxue-starterjaeger', '--region', 'us-east1', '--platform', 'managed']
images:
- gcr.io/aaxue-starter/jaeger
